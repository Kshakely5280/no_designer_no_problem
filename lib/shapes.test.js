const LogoPrompt = require("../index");

// A testing suite for Logo Prompt
describe("LogoPrompt", () => {
  describe("shapes", () => {
    it("should confirm a user has chosen a valid shape");
  });
});
